# Archive

This folder stores historical notes, debug output, backup files, and other reference material that should stay out of the live app structure.

Do not import files from here into runtime code. If something becomes active again, move it back into an appropriate live folder first.
