# Scripts

This folder is for terminal-run utilities and legacy operational scripts.

Guidelines:
- Keep reusable scripts here instead of the repository root.
- Use descriptive names based on the job the script performs.
- Put outdated or reference-only scripts under `scripts/legacy`.
